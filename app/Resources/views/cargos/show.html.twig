{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cargos</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cargo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cargo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cargos_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cargos_edit', { 'id': cargo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
