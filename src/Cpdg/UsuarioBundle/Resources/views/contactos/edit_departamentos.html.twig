{% extends 'base.html.twig' %}

{% block body %}

	{% if app.session.get('user').editarInformacion == 1 %}


	    <div class="col-xs-1 col-sm-1 col-md-2 col-lg-3">&nbsp;</div>
	    <div class="container col-xs-10 col-sm-10 col-md-8 col-lg-6"> 
	    <h4 class="modal-title"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Editar</h4>

	    {{ form_start(edit_form, {'action': '../' ~ app.request.get('id') ~ '/editfromdepartamento' }) }}
	        {{ form_widget(edit_form) }}
	        <button type="submit" class="btn btn-primary" data-dismiss="modal" onclick="window.history.back();"><i class="fa fa-backward" aria-hidden="true"></i> Cancelar </button>&nbsp;&nbsp;
	        <input type="submit" class="btn btn-primary" value="Guardar"></input>
	    {{ form_end(edit_form) }}
	    </div>

	{% else %}

		<div class="alert alert-warning">
		  <strong>Atención</strong> No tiene permisos para editar informacion.
		</div>

	{% endif %}

{% endblock %}
